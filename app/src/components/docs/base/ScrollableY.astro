---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"div">

const { class: className = "", ...rest } = Astro.props;
---

<div
  class={`overflow-hidden overflow-y-auto hfull ${className}`}
  {...rest}
>
  <slot />
</div>
